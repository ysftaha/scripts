#! /bin/sh


# modprobe acpi_call
# TODO git config files repository
# cpanm --force --sudo Text::Aspell
# cpanm --force --sudo HTML::Entities
# rm -r ~/.cpanm ~/.cpan
# 
# sym vdirsyncer calendars from .local/share/vdirsyncer/cal to ~/.config/khal
